<UserControl x:Class="EbookLibrary.Views.AddToListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cal="http://www.caliburnproject.org"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d"
             d:DesignHeight="680" d:DesignWidth="960">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Mode="PrimaryMid" Grid.Row="0" Padding="16,14">
            <DockPanel>
                <Button DockPanel.Dock="Left"
                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                        cal:Message.Attach="Back"
                        ToolTip="{DynamicResource String.Back}"
                        Foreground="White">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24"/>
                </Button>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                    <materialDesign:PackIcon Kind="BookmarkPlus" Width="28" Height="28"
                                            VerticalAlignment="Center" Margin="0,0,10,0"
                                            Foreground="White"/>
                    <TextBlock Text="{DynamicResource String.AddToList}" FontSize="20" FontWeight="Medium"
                               Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Book info -->
        <Border Grid.Row="1" Background="{DynamicResource MaterialDesignToolBarBackground}"
                BorderThickness="0,0,0,1" BorderBrush="{DynamicResource MaterialDesignDivider}"
                Padding="16,12">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="BookOutline" Width="18" Height="18"
                                         VerticalAlignment="Center" Margin="0,0,8,0" Opacity="0.6"/>
                <TextBlock Text="{Binding EbookTitle}" FontSize="14" FontWeight="Medium"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Reading lists -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Lists}" Margin="16,10,16,16">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="0,5,0,5" Padding="0">
                            <DockPanel>
                                <Border DockPanel.Dock="Left" Width="5"
                                        Background="{DynamicResource PrimaryHueMidBrush}"/>
                                <DockPanel Margin="14,12,6,12">
                                    <Button DockPanel.Dock="Right"
                                            Style="{StaticResource MaterialDesignRaisedButton}"
                                            cal:Message.Attach="[Event Click] = [Action SelectList($this)]"
                                            Margin="8,0,8,0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="BookmarkPlus" Width="16" Height="16"
                                                                     VerticalAlignment="Center" Margin="0,0,6,0"/>
                                            <TextBlock Text="{DynamicResource String.Add}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="15" FontWeight="SemiBold"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                            <materialDesign:PackIcon Kind="BookMultiple"
                                                                     Width="14" Height="14"
                                                                     VerticalAlignment="Center"
                                                                     Opacity="0.55" Margin="0,0,4,0"/>
                                            <TextBlock Text="{Binding Entries.Count}"
                                                       Style="{StaticResource MaterialDesignBody2TextBlock}"
                                                       Opacity="0.65" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </DockPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>

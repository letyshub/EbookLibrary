<UserControl x:Class="EbookLibrary.Views.ReadingListsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:cal="http://www.caliburnproject.org"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d"
             d:DesignHeight="680" d:DesignWidth="960">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Mode="PrimaryMid" Grid.Row="0" Padding="16,14">
            <DockPanel>
                <Button DockPanel.Dock="Left"
                        Style="{StaticResource MaterialDesignIconForegroundButton}"
                        cal:Message.Attach="Back"
                        ToolTip="{DynamicResource String.Back}"
                        Foreground="White">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24"/>
                </Button>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                    <materialDesign:PackIcon Kind="FormatListChecks" Width="28" Height="28"
                                            VerticalAlignment="Center" Margin="0,0,10,0"
                                            Foreground="White"/>
                    <TextBlock Text="{DynamicResource String.ReadingLists}" FontSize="20" FontWeight="Medium"
                               Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Create new list -->
        <Border Grid.Row="1" Background="{DynamicResource MaterialDesignToolBarBackground}"
                BorderThickness="0,0,0,1" BorderBrush="{DynamicResource MaterialDesignDivider}"
                Padding="16,10">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        cal:Message.Attach="CreateList"
                        Margin="8,0,0,0">
                    <TextBlock Text="{DynamicResource String.CreateList}"/>
                </Button>
                <TextBox x:Name="NewListName"
                         materialDesign:HintAssist.Hint="{DynamicResource String.NewListHint}"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         VerticalAlignment="Center" FontSize="14"/>
            </DockPanel>
        </Border>

        <!-- Lists -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Lists}" Margin="16,10,16,16">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="0,5,0,5" Padding="0">
                            <DockPanel>
                                <Border DockPanel.Dock="Left" Width="5"
                                        Background="{DynamicResource PrimaryHueMidBrush}"/>
                                <DockPanel Margin="14,12,6,12">
                                    <!-- Buttons -->
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                cal:Message.Attach="[Event Click] = [Action OpenList($this)]"
                                                ToolTip="{DynamicResource String.OpenList}"
                                                Foreground="{DynamicResource PrimaryHueMidBrush}">
                                            <materialDesign:PackIcon Kind="ArrowRight" Width="20" Height="20"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                cal:Message.Attach="[Event Click] = [Action DeleteList($this)]"
                                                ToolTip="{DynamicResource String.Delete}">
                                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18"/>
                                        </Button>
                                    </StackPanel>
                                    <!-- List info -->
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}"
                                                   FontSize="15" FontWeight="SemiBold"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                            <materialDesign:PackIcon Kind="BookMultiple"
                                                                     Width="14" Height="14"
                                                                     VerticalAlignment="Center"
                                                                     Opacity="0.55" Margin="0,0,4,0"/>
                                            <TextBlock Style="{StaticResource MaterialDesignBody2TextBlock}"
                                                       Opacity="0.65" VerticalAlignment="Center">
                                                <TextBlock.Text>
                                                    <Binding Path="Entries.Count"
                                                             StringFormat="{}{0}"/>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                </DockPanel>
                            </DockPanel>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
